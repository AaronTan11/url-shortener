<nav class="bg-gray-800 p-4">
  <div class="container mx-auto flex justify-between items-center">
    <%= link_to "URL Shortener", root_path, class: "text-white text-xl font-bold" %>
    <% if current_user %>
      <div class="flex items-center">
        <span class="text-white mr-4">Welcome, <%= current_user.username %></span>
        <%= button_to "Log Out", logout_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
      </div>
    <% else %>
      <div>
        <%= link_to "Log In", login_path, class: "text-white hover:text-gray-300 mr-4" %>
        <%= link_to "Sign Up", signup_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
      </div>
    <% end %>
  </div>
</nav>